# dotfiles

## Install

```
wget -O - http://dacs.run/dotfiles/lazy.sh | sh
```

or
 
```
cd ~ # optional
mkdir dev # optional
cd dev # optional
git clone https://github.com/felipedacs/dotfiles.git
cd dotfiles
chmod +x install.sh
./install.sh
```
